package xyz.deftu.template;

import net.minecraftforge.fml.common.Mod;

@Mod(name = ModInfo.NAME, version = ModInfo.VERSION, modid = ModInfo.ID)
public class ForgeTemplate {
    // TODO - Your mod's logic.
}